<h1><%= title %></h1>

<nav id="movies">
	<ul>
	<% for ( var i = 0; i < files.length; i++ ) { %>
		<li>
			<a href="<%= files[ i ] %>/"><%= files[ i ] %>
				<% if ( typeof movies[ files[ i ]].oflc === 'object' ) { %>
					<img src="/img/oflc/<%= movies[ files[ i ]].oflc.classification %>.png" alt="<%= movies[ files[ i ]].oflc.classification %>" />
				<% } %>
			</a>
			<small>(<a href="<%= files[ i ] %>/data">info</a>)</small>
		</li>
	<% } %>
	</ul>
</nav>


<figure>
	<img id="poster" />
</figure>


<script src="/ui/movieList.js"></script>
<script src="/ui/loadPoster.js"></script>
